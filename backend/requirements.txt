# Django and DRF
Django==5.0.1
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-filter==23.5

# PDF Processing (Pure Python - no system dependencies)
pdfplumber==0.10.3
PyPDF2==3.0.1

# PPTX Processing (Pure Python - no system dependencies)
python-pptx==0.6.23

# Word Document Generation (Pure Python - no system dependencies)
python-docx==1.1.0

# Celery for async tasks
# Note: With CELERY_TASK_ALWAYS_EAGER=True in settings, tasks run synchronously
# and Redis is NOT required. Only install redis if you want background processing.
celery==5.3.4
# redis==5.0.1  # Uncomment only if using Redis for background task processing

# Database
# psycopg2-binary==2.9.9  # Uncomment for PostgreSQL

# Authentication
djangorestframework-simplejwt==5.3.1

# Utilities
python-dotenv==1.0.0

# Dependencies that will be auto-installed:
# - Pillow (by pdfplumber/python-pptx) - Pure Python with optional C extensions
# - lxml (by python-docx/python-pptx) - Has pre-built wheels for Linux
